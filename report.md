# SmartCinema 智能影院选座系统 — 说明报告

## 分工：

**魏奕然**：基本功能设计和实现+现场汇报
**强之行**：模块1&2+加分项1&2+说明文档整合
**季节**：模块3&4+用户手册+PPT
**郑祺烨**（组长）：模块5&6+加分项3&4+设计文档

以下为各模块的实现说明，按模块顺序排列。

# SmartCinema 智能影院选座系统 — 基础框架说明

## 1. 项目简介

本项目是清华大学前端小学期大作业「SmartCinema 智能影院选座系统」的基础实现，已完成登录注册和主界面Canvas座位图（共30分）。后续模块可在此基础上扩展。

## 2. 文件结构

| 文件            | 说明                                                                                                                      |
| --------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `login.html`  | 登录注册入口页面。登录/注册 Tab 切换、普通用户注册、默认管理员账号登录、登录成功后跳转`cinema.html`                     |
| `cinema.html` | 影院选座主界面。Canvas 绘制弧形座位图、小/中/大厅三厅切换、座位状态（空座/选中/已售）、点击选座交互、显示当前登录用户信息 |
| `README.md`   | 本文档，说明基础框架的实现思路与后续扩展提示                                                                              |

## 3. 登录注册实现思路

### 3.1 无后端、纯前端存储

项目不使用后端数据库，用户信息存放在浏览器的 `localStorage` 中。

- 存储键名：`smartCinemaUsers`
- 数据结构：

```json
{
    "用户名": {
        "password": "密码",
        "role": "member" | "admin",
        "registerTime": "2026-07-20T13:45:51.000Z"
    }
}
```

### 3.2 默认管理员

系统初始化时（`login.html` 加载时），`initDefaultAdmin()` 会检查 `localStorage` 中是否已存在名为 `admin` 的用户。如果不存在，则自动创建：

> **用户名**：`admin`
> **密码**：`admin123`
> **角色**：`admin`

这是系统唯一的管理员账号，通过登录页直接输入 `admin / admin123` 即可登录。

### 3.3 普通用户注册

注册页面只收集用户名、密码、确认密码，不提供角色选择。所有新注册用户的 `role` 强制写死为 `"member"`（普通用户/会员）。

这意味着：

- 任何人都不能通过注册自行成为管理员
- 管理员只能由系统预设
- 符合真实系统中「角色不能由用户自己声明」的安全原则

### 3.4 登录验证流程

1. 读取用户名和密码输入
2. 从 `localStorage` 中读取 `smartCinemaUsers`
3. 检查用户名是否存在
4. 检查密码是否匹配
5. 登录成功后将会话写入 `sessionStorage`：
   - 键名：`smartCinemaCurrentUser`
   - 内容：`{ username, role, loginTime }`
6. 跳转至 `cinema.html?user=xxx&role=xxx`

### 3.5 会话管理

- `localStorage`：持久化存储，关闭浏览器后用户数据仍存在
- `sessionStorage`：会话级存储，关闭标签页/浏览器后自动清除，用于保存当前登录状态

### 3.6 登录状态在 cinema.html 中的使用

`cinema.html` 通过读取 URL 参数和 `sessionStorage` 判断当前登录用户，在页面顶栏显示用户名和角色标签：

- 普通用户：绿色「会员」标签
- 管理员：红色「管理员」标签

点击「登出」按钮会清除 `sessionStorage` 并返回 `login.html`。

## 4. 管理员账号登录方式

打开 `login.html`，在登录表单中输入：

> **用户名**：`admin`
> **密码**：`admin123`

点击「登录」即可进入 `cinema.html`，此时顶栏会显示「管理员」标签。

> **提示**：`admin` 账号的数据保存在 `localStorage` 中。如果曾经注册过同名用户，可能会与默认管理员冲突。清除浏览器 `localStorage` 或更换浏览器即可恢复默认。

## 5. Canvas 座位图实现思路

### 5.1 三个放映厅规格

| 放映厅 | 排数 | 每排座位 | 总座位 |
| ------ | ---- | -------- | ------ |
| 小厅   | 10   | 10       | 100    |
| 中厅   | 10   | 20       | 200    |
| 大厅   | 10   | 30       | 300    |

### 5.2 弧形排列计算

座位不是简单的矩形网格，而是沿圆弧扇形展开：

- 银幕位于画布顶部，对应圆心位置
- 第 1 排半径最小，离银幕最近，座位最密集
- 第 10 排半径最大，离银幕最远，座位最稀疏
- 左右对称分布，中间正对银幕

单个座位坐标使用极坐标转换得到：

```
radius = 基础半径 + 排号 × 排间距
angle  = -总跨度/2 + (列号 / (每排座位数-1)) × 总跨度
x = 圆心 x + radius × sin(angle)
y = 圆心 y + radius × cos(angle)
```

不同厅的弧形总跨度不同，座位越多跨度越大，以保证排列自然。

### 5.3 座位状态与颜色

| 状态         | 颜色 | 含义               |
| ------------ | ---- | ------------------ |
| `empty`    | 绿色 | 可选               |
| `selected` | 黄色 | 用户已选中但未付款 |
| `sold`     | 红色 | 已被购买           |

每个座位对象保存状态字段：`status = 'empty' | 'selected' | 'sold'`，绘制时根据 `status` 设置 `ctx.fillStyle`。

### 5.4 交互

- 点击空座 → 变为选中（黄）
- 再次点击选中座位 → 取消选中，变回空座（绿）
- 已售座位（红）不可点击
- 鼠标悬停时显示提示框，展示「X排X座 — 状态」

### 5.5 响应式

Canvas 使用 `devicePixelRatio` 进行高清渲染，尺寸随窗口 `resize` 自动重新计算，适配 PC、iPad、手机等不同屏幕。

## 6. 数据存储汇总

| 用途               | 存储方式             | 键名                       |
| ------------------ | -------------------- | -------------------------- |
| 所有用户信息       | `localStorage`     | `smartCinemaUsers`       |
| 当前登录会话       | `sessionStorage`   | `smartCinemaCurrentUser` |
| 座位状态（待扩展） | 建议`localStorage` | 例如`smartCinemaSeats`   |
| 订单记录（待扩展） | 建议`localStorage` | 例如`smartCinemaOrders`  |

## 7. 注意事项

- 本项目为前端教学演示，密码以明文存储于 `localStorage`。实际生产环境应使用后端数据库 + 密码哈希 + HTTPS
- 所有页面直接用浏览器打开即可运行，无需构建工具或服务器
- 清除浏览器 `localStorage` 会丢失所有用户数据和座位状态

作者：魏奕然
学号：2024012469
邮箱：yr-wei24@mails.tsinghua.edu.cn

---

---

# SmartCinema 智能影院选座系统 — 模块1&2及加分项1&2实现说明

姓名：强之行
学号：2024011816
院系：计算机系

## 1. 模块概览

本部分在基础框架（登录注册 + Canvas 座位图）之上，实现了 **智能推荐选座（模块1）** 和 **手动选座增强（模块2）**，完善了影院选座系统的核心交互流程。两个模块集成于 `cinema.html` 页面中，通过右侧边栏切换使用。

---

## 2. 模块1：智能推荐选座

### 2.1 功能描述

用户在右侧边栏「智能推荐」标签页中，选择票型、输入人数、填写每位成员的姓名和年龄，点击「推荐座位」按钮后，系统根据预设规则自动计算最佳区域和座位，并在 Canvas 座位图上以 **蓝色高亮（带闪烁边框）** 显示推荐结果。

### 2.2 支持的票型与输入

| 票型             | 人数范围  | 成员信息    | 推荐偏好                   |
| ---------------- | --------- | ----------- | -------------------------- |
| **个人票** | ≥1 人    | 姓名 + 年龄 | 尽量居中，兼顾中后排       |
| **情侣票** | 固定 2 人 | 姓名 + 年龄 | 优先中间区域连续双座       |
| **家庭票** | ≥2 人    | 姓名 + 年龄 | 优先中后排连续座位         |
| **团体票** | 5~20 人   | 姓名 + 年龄 | 同一排连续座位，优先中间排 |

- 所有票型均支持动态增减成员（先输入人数，自动生成对应数量的姓名/年龄输入框）。
- 年龄用于判断少年（<15岁）或老年（≥60岁），并作为硬约束依据。
- 团体票人数超出范围（5~20）时，前端给出明确错误提示。

### 2.3 排座规则

#### 硬约束（年龄避让，强制执行）

- **少年（＜15岁）**：禁止坐在前三排（第1~3排，索引0~2）。
- **老年（≥60岁）**：禁止坐在最后三排（倒数第1~3排）。
- 若团体/家庭中同时包含少年和老年，则取并集约束，即座位行必须同时避开前3排和后3排，只能坐中间区域。

#### 软偏好（评分排序，择优推荐）

1. 筛选出满足硬约束的所有行。
2. 在每行中扫描 **连续空座（绿色）** 段，选出长度 ≥ 所需人数的候选段。
3. 对每个候选段计算评分（分数越高越优），评分规则如下：
   - **情侣**：中心距离越小评分越高，连续段长度为偶数时额外加分。
   - **家庭**：靠近中后排（约总行数×0.6）加分，中心距离小加分。
   - **团体**：靠近中间排（总行数×0.5）加分，中心距离小加分。
   - **个人**：兼顾中间排和中心距离，综合评分。
4. 选取得分最高的候选段，将其中的座位标记为「推荐」（蓝色），并在结果框中显示具体排号和座位号。

若不存在任何满足硬约束且长度足够的连续空位，系统会提示错误，不进行高亮。

### 2.4 交互方式

- 点击「推荐座位」按钮后，算法会 **清除上一次的推荐高亮**，重新计算并显示新的推荐座位。
- 推荐座位（蓝色）**可以像空座一样被点击或拖拽**，一旦操作即变为「选中」（黄色），同时自动清除所有蓝色推荐高亮（表示用户已接受推荐或自行调整）。
- 再次点击推荐按钮，会基于当前座位状态（空座和已售）重新计算，不会保留之前的蓝色标记。

---

## 3. 模块2：手动选座增强

### 3.1 功能描述

在「手动选座」模式下，用户可直接在 Canvas 座位图上进行 **单选**、**多选（Ctrl+单击）** 和 **拖拽滑动选座**，自由选择心仪座位。所有手动操作均与推荐结果互操作。

### 3.2 操作方式

| 操作                               | 行为           | 状态变化                                                                          |
| ---------------------------------- | -------------- | --------------------------------------------------------------------------------- |
| **单击座位**                 | 切换状态       | 空座/推荐 → 选中（黄）；选中 → 空座（绿）；已售不变                             |
| **Ctrl + 单击座位**          | 多选切换       | 与单击逻辑相同，便于同时切换多个座位                                              |
| **拖拽滑动（按住左键滑动）** | 画笔连续切换   | 滑过的座位按「空座→选中，选中→空座」切换，已售跳过；可快速连续选中/取消多个座位 |
| **清空所有选中**             | 按钮（侧边栏） | 将所有黄色座位恢复为空座，同时清除推荐高亮                                        |

- 拖拽选座在桌面端使用鼠标实现，移动端虽无触摸拖拽，但可通过单击完成所有功能。
- 所有操作后，座位状态**立即保存至 LocalStorage**（但黄色状态在保存时会被映射为空座，因此刷新页面后黄色消失，而红色已售保留）。

### 3.3 与推荐座位的交互

- 当推荐座位高亮（蓝色）出现后，用户可以通过 **单击或拖拽** 将任意蓝色座位变为黄色（选中），此时系统自动清除所有剩余蓝色高亮，并隐藏推荐结果框。
- 若用户对推荐结果不满意，也可直接手动点击其他空座进行选择，推荐高亮不会自动消失，但一旦操作任何座位（包括点击或拖拽），都会清除推荐高亮，避免干扰。

---

## 4. 数据持久化与状态管理

- **用户数据**：`localStorage` 键 `smartCinemaUsers`，存储所有注册用户信息。
- **座位状态**：`localStorage` 键 `smartCinemaSeats`，按影厅（small/medium/large）分别存储二维状态数组。
- **持久化规则**：
  - `EMPTY`（绿色）和 `SOLD`（红色）状态 **永久保存**。
  - `SELECTED`（黄色）和 `RECOMMENDED`（蓝色）状态 **不保存**：保存时自动映射为 `EMPTY`。
- **设计意图**：选中（黄）仅是临时操作，刷新页面后应重置为空座；已售（红）为真实销售数据，必须持久化。

---

## 5. 交互流程示例

1. **登录** → 进入 `cinema.html`，默认显示「智能推荐」面板。
2. **推荐**：选择「家庭票」，人数 3，年龄分别为 40、38、12 岁（少年），点击推荐 → 系统自动避开前三排，在中后排推荐连续 3 个空座，蓝色高亮。
3. **手动调整**：用户觉得推荐位置偏左，切换到「手动选座」，单击偏右的座位选中，或按住 Ctrl 多选，蓝色高亮自动清除。
4. **拖拽选座**：按住鼠标左键在座位区滑动，快速选中一排座位。
5. **切换影厅**：点击「中厅」，所有座位状态重新加载（黄色清空），推荐高亮消失。
6. **刷新页面**：所有黄色消失，红色已售状态保留，空座保持。

---

## 6. 技术实现要点

- **状态扩展**：新增 `RECOMMENDED = 3` 状态，并在 `COLORS` 中定义蓝色。
- **闪烁边框**：推荐座位采用 `setInterval` 定时切换边框颜色，营造呼吸效果，提示用户。
- **推荐算法**：采用「硬过滤 → 候选段扫描 → 评分排序」三步策略，兼顾约束和偏好。
- **拖拽选座**：监听 `mousedown`/`mousemove`/`mouseup`，使用 `lastDraggedSeat` 防止同一座位重复切换，提升流畅度。
- **存储映射**：在 `saveSeatsToLocal` 中将 `SELECTED` 和 `RECOMMENDED` 统一转为 `EMPTY`，实现黄色不持久化。

---

## 7. 注意事项

- 推荐算法基于 **当前座位状态**（空座和已售）计算，黄色座位（临时选中）不会被纳入推荐（因为保存时会被视为空座，但运行时它们仍为黄色，算法只扫描 `EMPTY`）。
- 团体票必须同排连续，若人数超出某排空段长度，算法会尝试其他排，若所有排均不满足则报错。
- 年龄约束同时作用于所有成员，团体中若既有少年又有老年，则必须避开前3排和后3排，只能坐中间区域。

---

## 8. AI观影问答式顾问与交互增强（加分项1、2）

### 8.1 AI 观影问答式顾问

#### 8.1.1 功能入口

页面右下角显示 **🤖 机器人图标**，点击展开悬浮对话窗口，再次点击收起。窗口可拖动（由浏览器默认行为支持）。

#### 8.1.2 对话交互流程

- **首次对话**：AI 主动询问偏好，引导用户输入需求（如"中间靠后、安静"）。
- **意图解析**：系统通过关键词识别用户对 **视角**、**距离**、**安静度**、**过道便捷性**、**宽敞度** 的偏好，支持多轮槽位填充。
- **自动推荐**：根据偏好动态调整评分权重，遍历当前影厅所有空座，计算综合得分，选出最优座位（支持指定人数，默认1人）。
- **推荐理由**：输出包含视角评价、距离评价、安静度、便捷性等维度的自然语言描述。
- **采纳推荐**：用户输入"采纳"或点击对话中的提示，AI 会自动将推荐座位置为选中（黄色），并清除高亮。

#### 8.1.3 技术实现

- **纯规则引擎**：不依赖任何外部 API，基于关键词匹配和权重模型实现"智能"。
- **评分维度**：复用原有 `calculateSeatExperience()` 获取视角、距离、空座率，新增"过道便捷性"计算（基于列距）。
- **动态权重**：根据用户偏好（如"安静"提升空座权重至50%）实时调整。
- **视觉反馈**：推荐座位在座位图上叠加蓝色脉冲光晕（由动画层绘制），不修改座位状态，不影响原有推荐逻辑。

### 8.2 交互增强

#### 8.2.1 Shift+框选（桌面端）

- **操作方式**：在手动选座模式下，按住 `Shift` 键，在座位图上拖拽鼠标绘制矩形框。
- **行为**：释放鼠标后，框内所有状态为 `EMPTY` 或 `RECOMMENDED` 的座位自动变为 `SELECTED`（黄色）。
- **视觉辅助**：框选过程中实时显示半透明虚线矩形，清晰指示选区范围。

#### 8.2.2 触摸滑动选座（移动端）

- **操作方式**：在触摸屏上，手指在座位图上滑动。
- **行为**：手指滑过的座位按"空座→选中，选中→空座"切换，与桌面端拖拽滑动逻辑一致。
- **适配**：自动阻止页面滚动，避免干扰选座操作。

#### 8.2.3 独立动画叠加层

- **技术实现**：在主 Canvas 之上叠加一个透明 Canvas（`pointer-events: none`），由独立的 `requestAnimationFrame` 循环驱动。
- **绘制内容**：
  - AI 推荐座位的脉冲光晕（呼吸效果，频率 2.5Hz）。
  - Shift 框选时的矩形边框和半透明填充。
- **性能**：动画层独立于主画布重绘，不影响主画布绘制性能，且在主画布尺寸变化时自动适配。

### 8.3 与原有系统的协作关系

- **零侵入**：所有新增功能通过独立的 DOM 元素、样式和 JavaScript 模块注入，不修改任何原有的函数、事件监听、样式或全局变量。
- **数据读取**：AI 推荐和框选均直接读取全局 `seats` 数组，并调用现有的 `saveSeatsToLocal()`、`updateSelectedCount()`、`draw()` 等函数，确保状态一致。
- **模式兼容**：AI 顾问在任意功能模式下均可使用（热度地图模式下仍可对话，但推荐只针对空座）；Shift 框选和触摸滑动仅在手动选座模式下生效（与原有规则一致）。
- **无障碍兼容**：AI 高亮和框选动画自动适配高对比度、大字体、色盲友好模式（通过 CSS 变量和主题检测）。

---

---

# SmartCinema 智能影院选座系统 — 模块3 & 4 实现说明

## 1. 模块概览

本部分在基础框架及模块1、2之上，实现了影院热度地图（模块3）和观影体验评分（模块4），集成于`cinema.html`右侧功能栏。

## 2. 模块3：影院热度地图

### 2.1 功能描述

用户点击右侧「热度地图」标签后，原 Canvas 座位图切换为热度分析视图：

- **红色**：热门座位，热度70～100；
- **黄色**：一般座位，热度45～69；
- **蓝色**：冷门座位，热度0～44。

座位颜色和周围的半透明光晕都由 Canvas 原生 API 绘制。鼠标悬停时会显示座位号、热度数值和分类。

### 2.2 一周动态变化

侧边栏提供星期一至星期日滑块和「播放一周变化」按钮：

1. 拖动滑块可以直接查看某一天的热度；
2. 点击播放按钮后，系统每1.1秒切换一天；
3. 离开热度地图标签时自动停止播放，避免后台定时器继续运行；
4. 最近选择的星期保存在 `localStorage` 的 `smartCinemaHeatmapDay` 中。

### 2.3 热度算法

每个座位的热度由四部分组成：

1. **排距偏好**：以中后排为主要热门区，不同日期的理想排位有轻微变化；
2. **中心偏好**：越接近银幕中轴，热度越高；
3. **星期修正**：周末整体需求高于工作日；
4. **稳定扰动**：使用座位行、列和星期生成固定伪随机值，使同一区域内也有自然差异，同时保证同一天重复查看结果一致。

实现中使用高斯衰减函数模拟“离理想位置越远，热度越低”的规律，再将结果限制在0～100范围。所有计算均在浏览器本地完成。

### 2.4 Canvas 绘制方式

- `drawHeatmapLayer()` 为每个座位绘制径向渐变光晕；
- `drawSeat()` 在热度模式下改用热度色板；
- `globalCompositeOperation = "screen"` 使相邻光晕自然叠加；
- 影厅切换后会基于新影厅座位数重新统计热门、一般和冷门座位数量。

---

## 3. 模块4：观影体验评分

### 3.1 使用流程

1. 在「智能推荐」或「手动选座」中选中一个或多个座位；
2. 切换到「体验评分」，查看系统分、等级和三项指标；
3. 观影后选择1～5星并提交，查看综合结果。

未选座时评分面板不会给出无意义分数，而是提示用户先完成选座。

### 3.2 系统评分指标

| 指标       | 权重 | 计算思路                                          |
| ---------- | ---: | ------------------------------------------------- |
| 视角舒适度 |  40% | 座位越靠近银幕中轴得分越高，最侧边座位得分降低    |
| 银幕距离   |  35% | 以总排数约58%的位置作为舒适距离，过近或过远均扣分 |
| 周围空座   |  25% | 统计座位周围最多8个邻位，空座比例越高得分越高     |

多选座位时，先分别计算每个座位的三项指标，再取平均值生成整组座位的系统分。

### 3.3 等级划分

| 分数范围 | 结果 |
| -------- | ---- |
| 85～100  | 极佳 |
| 70～84   | 优秀 |
| 0～69    | 一般 |

系统同时根据三项指标生成自然语言分析，说明视角、排距和周围空间的优缺点，帮助用户理解评分逻辑。

### 3.4 观众评分与综合结果

观众可选择1～5星，星级先换算为百分制，再与系统分合成：

```text
综合分 = 系统分 × 70% + 观众分 × 30%
观众分 = 星级 × 20
```

系统分占较高权重，用于保持座位客观条件的稳定性；观众分反映实际观影感受。综合分仍按「极佳、优秀、一般」三档输出。

### 3.5 数据持久化

评分记录保存在 `localStorage` 的 `smartCinemaExperienceRatings` 中，以“用户 + 影厅 + 座位组合”为键，内容包括：

- 观众星级和百分制分数；
- 系统分、综合分和等级；
- 影厅、座位列表；
- 保存时间。

用户再次选择同一组座位时，系统会自动恢复此前保存的评分结果。

---

## 4. 与原有模块的协作关系

- 模块3复用模块1、2的 `seats` 二维数组和 Canvas 布局坐标，只改变分析视图，不修改座位状态。
- 热度模式下禁止点击或拖拽选座，避免用户把热度颜色误认为座位状态。
- 模块4直接读取模块1推荐后确认的黄色座位，或模块2手动选择的黄色座位。
- 切换影厅时，系统重新计算热度统计并清空当前临时选座评分面板。
- 已售、选中、推荐状态仍沿用原有 `smartCinemaSeats` 持久化规则。

---

## 5. 响应式与交互设计

- 右侧功能栏扩展为四个按钮组成的2×2网格，避免窄屏文字拥挤。
- PC端使用左右布局；屏幕宽度小于860px时，座位图与功能面板改为上下布局。
- 热度统计使用三列信息卡；评分使用圆环总分、进度条指标和星级按钮。
- 所有核心流程控制在3步以内，符合“Don't Make Me Think”设计原则。

---

## 6. 验证记录

已完成以下本地自动化检查：

- JavaScript 语法检查通过；
- 页面脚本引用的 DOM `id` 均存在；
- 小厅、中厅热度分类数量分别与100座、200座一致；
- 周一同时存在热门、一般、冷门三档座位；
- 星期六总热度高于星期一，且各座位热度随日期变化；
- 系统评分三项指标与总分均保持在0～100；
- 1～5星提交后按70%/30%公式正确生成综合分；
- 评分记录成功写入 `smartCinemaExperienceRatings`。

测试脚本位于 `.qa/smartcinema-smoke-tests.mjs`，可使用项目工作区提供的 Node.js 运行。

---

---

# SmartCinema 智能影院选座系统 — 模块5 & 6 实现说明

姓名：郑祺烨
学号：2024012573
班级：未央-软件41

## 1. 模块概览

本部分在基础框架及模块1～4之上，实现了：

- **模块5：无障碍模式**——大字体模式、高对比度模式、色盲友好模式、可选语音提示，覆盖视觉障碍和阅读障碍用户的核心需求。
- **模块6：订单中心**——预订座位、取消预订、立即支付（购票）、申请退票，完整的订单生命周期管理，数据全部持久化在 `localStorage` 中。

两个模块均深度集成在 `cinema.html` 中，与原有座位图、手动选座、智能推荐等模块无缝协作，无需第三方库或后端服务。

---

## 2. 模块5：无障碍模式

### 2.1 功能入口

页面顶部右侧增加「♿ 无障碍」按钮，点击后弹出无障碍设置面板，包含四个独立开关：

1. **大字体模式**：全局放大文字与选座图
2. **高对比度模式**：纯黑背景 + 白色边框 + 黄/红/绿高亮
3. **色盲友好模式**：基于 Okabe & Ito 配色的色盲安全色板 + 几何图案辅助
4. **语音提示**：关键操作的中文语音播报

所有设置独立开关、可任意组合，状态自动保存，刷新页面后自动恢复。

### 2.2 大字体模式

开启后全局字体放大约 25%～40%，主要调整包括：

| 区域            | 放大效果                                         |
| --------------- | ------------------------------------------------ |
| 标题/按钮文字   | 字号增大 25%                                     |
| 输入框/下拉选项 | 字号与行高同步增大                               |
| Canvas 选座图   | 整体放大 1.25 倍（座位、行号、银幕均按比例放大） |
| 座位图例        | 圆点与文字同步放大                               |
| 订单面板        | 卡片内边距、字体、按钮尺寸整体增大               |

实现方式：通过给 `<body>` 添加 `large-text` 类，由 CSS 选择器统一控制；Canvas 部分通过 `resizeCanvas()` 中的 `scale` 参数重新计算布局。

### 2.3 高对比度模式

针对低视力用户，最大化前景与背景的对比度：

- **背景**：纯黑 `#000000`
- **边框/文字**：纯白 `#ffffff`
- **重要信息**：亮黄 `#ffff00`（标题、选中状态、标签）
- **错误/警示**：亮红 `#ff0000`
- **成功/确认**：亮绿 `#00ff00`
- **所有按钮**：2px 白色粗边框

所有交互元素都有清晰的边界区分，避免用户混淆可点击区域。Canvas 座位图同步切换为高对比色板。

### 2.4 色盲友好模式

基于 Okabe & Ito 八色通用色盲安全调色板，重新设计座位状态配色：

| 状态   | 原配色          | 色盲友好配色         | 辅助图案 |
| ------ | --------------- | -------------------- | -------- |
| 空座   | 绿色`#43a047` | 蓝色`#0072B2`      | 圆点     |
| 选中   | 橙色`#ffb300` | 琥珀色`#E69F00`    | 条纹     |
| 已售   | 红色`#e53935` | vermilion`#D55E00` | 十字     |
| 推荐   | 蓝色`#42a5f5` | 天蓝`#56B4E9`      | 圆环     |
| 已预订 | 紫色`#9c27b0` | 紫红`#CC79A7`      | 菱形     |

**双重编码策略**：每种座位状态不仅颜色不同，还搭配独特的几何图案（dots / stripes / cross / circle / diamond），即使完全无法分辨颜色的用户也能通过图案区分状态。图案通过 Canvas 原生绘制在座位内部。

热度地图也同步使用色盲友好的三档配色（蓝/黄/红），保证所有功能模块的一致性。

### 2.5 语音提示

基于 Web Speech API（`SpeechSynthesis`）实现中文语音播报，无需额外依赖：

**播报场景：**

- 切换影厅（小厅/中厅/大厅）
- 切换功能模式（智能推荐/手动选座/热度地图/体验评分）
- 选座操作（选中/取消选中）
- 推荐座位成功/失败
- 预订成功/取消
- 购票成功/退票成功
- 登录注册页面的状态提示

语音功能默认关闭，用户可在无障碍面板中主动开启；开启后所有关键操作都有清晰的语音反馈，帮助视障用户理解当前操作结果。

### 2.6 数据持久化

无障碍设置保存在 `localStorage` 的 `smartCinemaA11ySettings` 中：

```json
{
  "largeText": false,
  "highContrast": false,
  "colorblind": false,
  "voice": false
}
```

`cinema.html` 和 `login.html` 两个页面共享同一套设置，用户在任一页面修改后，另一页面刷新即可同步。

---

## 3. 模块6：订单中心

### 3.1 功能入口

页面顶部右侧增加「📋 我的订单」按钮，点击后弹出订单列表面板，展示当前登录用户的所有历史订单。

订单面板与座位图同屏显示，用户可以一边看座位一边管理订单，无需跳转新页面。

### 3.2 座位状态扩展

在原有三种状态基础上新增「已预订」状态：

| 状态   | 常量                | 颜色 | 含义             |
| ------ | ------------------- | ---- | ---------------- |
| 空座   | `EMPTY` (0)       | 绿色 | 可自由选择       |
| 选中   | `SELECTED` (1)    | 黄色 | 临时选中，未提交 |
| 已售   | `SOLD` (2)        | 红色 | 已完成购票       |
| 推荐   | `RECOMMENDED` (3) | 蓝色 | 算法推荐位       |
| 已预订 | `RESERVED` (4)    | 紫色 | 已下单未支付     |

已预订座位与已售座位一样不可点击，避免重复预订。

### 3.3 预订流程

1. 用户在「手动选座」或「智能推荐」中选择座位（黄色）；
2. 点击「🎫 预订座位」按钮；
3. 系统生成唯一订单号，创建状态为 `reserved`（待支付）的订单；
4. 选中座位状态变为「已预订」（紫色），其他用户不可再选；
5. 弹出预订成功提示，显示订单号、座位数、金额；
6. 自动打开「我的订单」面板，用户可继续支付或取消。

**未登录处理**：如果用户未登录，点击预订按钮会提示登录并跳转至 `login.html`。

### 3.4 订单操作

每个订单根据状态提供不同的操作按钮：

| 订单状态           | 可用操作 | 操作结果                           |
| ------------------ | -------- | ---------------------------------- |
| 待支付 (reserved)  | 立即支付 | 订单变为「已完成」，座位变为已售   |
| 待支付 (reserved)  | 取消预订 | 订单变为「已取消」，座位恢复为空座 |
| 已完成 (completed) | 申请退票 | 订单变为「已退票」，座位恢复为空座 |
| 已取消 (cancelled) | 无       | 终态，仅可查看                     |
| 已退票 (refunded)  | 无       | 终态，仅可查看                     |

所有操作均有二次确认（`confirm` 对话框），防止误操作。

### 3.5 订单数据结构

订单保存在 `localStorage` 的 `smartCinemaOrders` 中，数据结构：

```json
{
  "id": "ORD1784910366957189",
  "username": "test",
  "hall": "small",
  "hallName": "小厅",
  "seats": [
    { "row": 4, "col": 4 }
  ],
  "status": "reserved",
  "price": 50,
  "createTime": "2026-07-24T16:26:06.957Z",
  "payTime": null,
  "cancelTime": null,
  "refundTime": null
}
```

字段说明：

- `id`：订单号，格式为 `ORD` + 时间戳 + 随机数，全局唯一
- `status`：`reserved` / `completed` / `cancelled` / `refunded`
- `price`：单价 ¥50 × 座位数
- `seats`：座位数组，每项包含 `row`（排号，1-based）和 `col`（座号，1-based）

### 3.6 价格体系

- 单座票价：**¥50**
- 总金额 = 座位数 × 50
- 退票全额退款，无手续费

### 3.7 用户隔离

- 订单列表只显示当前登录用户自己的订单
- 取消预订、支付、退票等操作均校验 `order.username === currentUser.username`
- 不同用户的数据完全隔离，符合真实系统的安全要求

### 3.8 座位状态持久化

座位状态（空座/已售/已预订）保存在 `localStorage` 的 `smartCinemaSeats` 中：

- `SELECTED`（选中）和 `RECOMMENDED`（推荐）是**临时状态**，不持久化，刷新后自动恢复为空座
- `SOLD`（已售）和 `RESERVED`（已预订）是**持久状态**，保存到 `localStorage`
- 切换影厅、刷新页面后，已售和已预订座位保持不变

这样既保证了已售/已预订座位的稳定性，又避免了临时选中状态长时间占用座位。

---

## WebSocket 实时座位更新（多人在线模拟）

### 功能概述

由于本系统为纯前端项目，无后端服务器支持，因此采用 **BroadcastChannel API** 模拟 WebSocket 的实时通信能力，实现同一浏览器多标签页之间的实时座位状态同步。当 `BroadcastChannel` 不可用时，自动降级为 `localStorage` 事件监听。

### 通信架构

```
┌──────────────┐     BroadcastChannel      ┌──────────────┐
│  标签页 A     │ ◄──────────────────────► │  标签页 B     │
│  (用户 Alice) │      seat_batch_change    │  (用户 Bob)   │
│              │      presence_ping         │              │
└──────────────┘      presence_join         └──────────────┘
       │                                        │
       │          localStorage 降级              │
       └──────────── storage event ─────────────┘
```

### 消息类型

| 消息类型              | 方向 | 说明                                       |
| --------------------- | ---- | ------------------------------------------ |
| `seat_batch_change` | 广播 | 预订/购票/退票/取消预订等批量状态变更      |
| `presence_ping`     | 广播 | 3 秒间隔心跳，维持在线状态                 |
| `presence_join`     | 广播 | 新标签页打开时通知所有人                   |
| `presence_leave`    | 广播 | 标签页关闭时通知所有人（`beforeunload`） |

### 同步规则

**只同步全局状态变更，不同步个人选中操作：**

| 操作                  | 是否广播         | 原因             |
| --------------------- | ---------------- | ---------------- |
| 点击选中/取消选中     | **不同步** | 属于个人临时操作 |
| 拖拽选座              | **不同步** | 属于个人临时操作 |
| 清空所有选中          | **不同步** | 属于个人临时操作 |
| 预订座位 (→RESERVED) | **同步**   | 全局状态变更     |
| 取消预订 (→EMPTY)    | **同步**   | 全局状态变更     |
| 支付购票 (→SOLD)     | **同步**   | 全局状态变更     |
| 退票 (→EMPTY)        | **同步**   | 全局状态变更     |

这个设计确保每个用户的选座体验独立不受干扰，只有真正完成预订/购票后才会通知所有在线用户更新座位状态。

### 在线用户感知

页面头部显示绿色在线徽章（如 `🟢 2 在线`），实时反映当前打开的标签页数量：

- **心跳机制**：每个标签页每 3 秒更新自己的时间戳到 `localStorage` 的 `smartCinemaPresence` 中
- **离线检测**：超过 8 秒未更新心跳的客户端自动从在线列表中移除
- **加入/离开通知**：新标签页打开时弹出 Toast 提示 "XXX 加入了选座大厅"；标签页关闭时自动清理

### 冲突检测

当远程收到座位批量变更消息时，遍历变更座位列表，检查当前用户是否有选中的座位被他人预订/购买：

- 如果发现冲突（本地 SELECTED + 远程 SOLD/RESERVED），弹出红色警告 Toast：
  > ⚠️ 你选中的 X排Y座 已被其他用户购买
  >
- 同时触发语音提示（如语音功能已开启）
- 本地座位状态自动更新为远程最新状态，选中的座位列表同步刷新

### Toast 通知系统

页面右下角实时显示座位变更通知：

- **普通通知**（蓝色左边框）：显示变更座位的排号、座号和新状态
- **冲突警告**（红色左边框）：选中座位被他人购买
- 动画效果：从右侧滑入，3 秒后自动滑出消失
- 单次最多展示 3 个座位详情，超过部分显示 "等 N 个座位"

### 测试方法

1. 在浏览器中打开 `cinema.html`（标签页 A）
2. 复制 URL，在新标签页中打开（标签页 B）
3. 在标签页 A 中预订并支付一个座位
4. 标签页 B 中实时看到该座位变为红色（已售），并收到 Toast 通知
5. 两个标签页的在线徽章均显示 `🟢 2 在线`

### 数据持久化

- 在线状态：`localStorage` → `smartCinemaPresence`
- 客户端 ID：`sessionStorage` → `smartCinemaClientId`（每个标签页唯一）

---

## 个性化界面（双主题切换）

### 功能概述

系统提供两种界面风格，用户可一键自由切换，满足不同光线环境和个人偏好：

| 风格                       | 说明                                        |
| -------------------------- | ------------------------------------------- |
| **暗夜风格**（默认） | 深蓝黑底色 + 亮蓝色强调，适合暗光环境，护眼 |
| **日间风格**         | 浅灰白底色 + 深蓝色强调，适合明亮环境，清晰 |

### 切换方式

页面头部设有 **"🌓 风格"** 按钮，点击即可在两种风格间即时切换。按钮文字会随当前主题更新：

- 暗夜模式 → 显示 "🌙 暗夜"
- 日间模式 → 显示 "☀️ 日间"

主题选择保存在 `localStorage` 中（key: `smartCinemaTheme`），刷新页面或重新打开后保持选择。

### 技术实现

采用 **CSS 自定义属性（变量）** 实现主题切换，避免维护两套独立样式表：

```
body {                          body[data-theme="light"] {
  --bg-body: #0a0a1a;            --bg-body: #f0f2f5;
  --text-primary: #e0e0e0;       --text-primary: #1a1a1a;
  --bg-surface: #12122a;         --bg-surface: #ffffff;
  --accent: #64b5f6;             --accent: #1565c0;
  --border-subtle: rgba(...);     --border-subtle: rgba(0,0,0,.08);
  ...                             ...
}                               }
```

共定义了 **80+ 个 CSS 变量**，覆盖：

- 背景色（body / header / sidebar / footer / 卡片 / 输入框）
- 文字色（标题 / 正文 / 次要文字 / 标签 / 提示）
- 边框色（各级边框透明度不同）
- 强调色（主色 / 辅色 / 发光色）
- 按钮（主按钮渐变 / 次要按钮边框 / 悬浮效果）
- 面板（订单 / 无障碍设置 / 提示 Tooltip）
- 评分模块（环形图 / 进度条 / 星标 / 文字）
- 热度统计（卡片背景 / 边框 / 标签）
- 通知组件（Toast 背景 / 边框）

### Canvas 选座图适配

Canvas 不是 DOM 元素，无法直接使用 CSS 变量，因此在 `draw()` 函数中通过 JavaScript 读取当前主题：

```javascript
// Canvas 背景色跟随主题
ctx.fillStyle = getComputedStyle(document.body)
    .getPropertyValue('--bg-body').trim();

// 银幕文字跟随主题
const isLight = document.body.getAttribute('data-theme') === 'light';
ctx.fillStyle = isLight
    ? 'rgba(40, 40, 40, 0.9)'
    : 'rgba(255, 255, 255, 0.85)';
```

切换主题时自动调用 `resizeCanvas()` 重绘 Canvas，确保背景色和文字色立即生效。

### 与无障碍模式的兼容

双主题与高对比度模式、大字体模式、色盲友好模式完全兼容：

- 高对比度模式优先级最高，使用固定的高对比色值覆盖主题变量
- 大字体模式仅调整字号和 Canvas 缩放，不影响颜色
- 色盲友好模式仅影响座位颜色和图案，不影响全局主题
- 四种模式可任意组合，互不冲突

### 跨页面同步

`cinema.html` 和 `login.html` 共享同一套主题变量和 `localStorage` key，在任一页面切换主题后，另一页面刷新即可同步。

---

## 4. 与原有模块的协作关系

### 4.1 与手动选座的协作

- 手动选座模式下新增「🎫 预订座位」按钮
- 已预订座位（紫色）与已售座位（红色）一样不可点击、不可拖拽选座
- 座位 tooltip 同步新增「已预订」状态文字
- 底部图例新增「已预订」色块说明

### 4.2 与智能推荐的协作

- 智能推荐推荐的座位如果是已预订状态，会自动跳过
- 推荐结果（蓝色）可以直接点击预订，流程与手动选座一致

### 4.3 与热度地图的协作

- 热度地图模式下，已预订座位仍按热度颜色显示，不影响热度分析
- 热度模式下禁止点击选座的规则仍然有效

### 4.4 与体验评分的协作

- 体验评分可以对已预订座位进行评分（已预订座位客观条件不变）
- 评分记录与订单独立存储，互不影响

### 4.5 与无障碍模式的协作

- 订单面板在大字体模式下自动增大字体和内边距
- 订单面板在高对比度模式下使用高对比配色
- 预订、取消、支付、退票等操作均支持语音提示
- 色盲友好模式下，已预订座位使用菱形图案 + 紫红配色

---

## 5. 响应式与交互设计

- 无障碍设置面板采用浮动卡片设计，从右上角展开，不遮挡主操作区
- 订单面板同样采用右侧浮动卡片，宽度 380px，大字体模式下增至 440px
- 订单卡片内信息层次清晰：顶部订单号+状态，中间座位/金额/时间，底部操作按钮
- 状态标签使用不同颜色快速区分（橙=待支付，绿=已完成，灰=已取消，红=已退票）
- 所有操作按钮语义明确（「立即支付」「取消预订」「申请退票），符合用户直觉
- 核心操作流程控制在 2～3 步以内，例如「选座 → 预订 → 支付」仅需 3 次点击
