
# SmartCinema 设计文档

## 一、设计风格说明

### 1.1 整体定位

SmartCinema 是一个面向影院选座场景的 Web 工具应用，整体设计风格定位为**深色科技风**——以深蓝黑为基底，蓝色系为品牌主色调，融合微光感（neon-glow）细节，营造沉浸式、专业化的操作体验。界面风格同时兼顾**无障碍包容性设计**，为视障、色盲等用户群体提供大字体、高对比度、色盲友好及语音辅助四种模式。

### 1.2 核心设计原则

- **深底浅字**：大面积深色背景降低长时间使用时的视觉疲劳，浅色文字保证信息可读性。
- **色彩语义化**：每种颜色在系统中承载明确的功能含义（操作、状态、警告），降低用户认知负担。
- **微动效反馈**：所有交互元素统一使用 `transition: all 0.2s` 过渡动画，提供流畅的操作反馈而不干扰用户。
- **双重编码**：关键状态信息同时通过颜色与形状/图案传达，确保色盲用户也能准确识别。
- **零外部依赖**：纯原生 HTML/CSS/JS 实现，无框架、无构建工具，浏览器直接打开即可运行。

### 1.3 技术实现方式

所有样式以内嵌 `<style>` 标签方式编写于 `login.html` 和 `cinema.html` 中，无独立 CSS 文件。座位图通过 HTML5 Canvas 2D API 绘制，其余界面采用 Flexbox + Grid 实现布局。

---

## 二、色彩方案

### 2.1 主题底色系（深色模式）

深色底色贯穿全站，分为三个层级：

| 层级     | 色值        | 用途                    |
| -------- | ----------- | ----------------------- |
| 页面背景 | `#0a0a1a` | `<body>` 根背景       |
| 容器背景 | `#12122a` | Header、Footer、Sidebar |
| 组件背景 | `#1a1a3e` | 输入框、面板、卡片      |

### 2.2 品牌主色（蓝色系）

蓝色作为 SmartCinema 的品牌识别色，贯穿 Logo、主按钮、标题、激活态、进度条等元素：

| 色阶    | 色值        | 用途                               |
| ------- | ----------- | ---------------------------------- |
| Lighter | `#64b5f6` | Logo 渐变亮端、标题色、信息强调线  |
| Base    | `#42a5f5` | 主按钮渐变起端、激活边框、聚焦状态 |
| Darker  | `#1e88e5` | 主按钮渐变收端、进度条起端         |

典型渐变组合：`linear-gradient(135deg, #42a5f5, #1e88e5)`

### 2.3 功能语义色

每种颜色对应明确的功能或状态含义：

| 语义      | 色值                                    | 使用场景                              |
| --------- | --------------------------------------- | ------------------------------------- |
| 成功/可用 | `#43a047` / `#66bb6a` / `#4caf50` | 空座填充、登录按钮、已完成订单        |
| 警告/选中 | `#ffb300` / `#ff8f00` / `#fbc02d` | 选中座位、待支付订单、热度warm        |
| 危险/已售 | `#e53935` / `#c62828` / `#ef5350` | 已售座位、退票按钮、热度hot、错误提示 |
| 推荐/信息 | `#42a5f5` / `#1e88e5` / `#56b4e9` | 推荐座位、热度cool、信息框            |
| 特殊/预定 | `#9c27b0` / `#7b1fa2` / `#cc79a7` | 已预订座位                            |

### 2.4 文字色阶

| 层级 | 色值                | 用途                       |
| ---- | ------------------- | -------------------------- |
| 正文 | `#e0e0e0`         | 主体文字                   |
| 次要 | `#aaa`            | 标签、辅助说明             |
| 弱化 | `#888` / `#666` | 占位符、禁用态、极次要信息 |

### 2.5 边框色

统一使用半透明蓝紫调：`rgba(100, 140, 255, 0.15)` ~ `rgba(100, 140, 255, 0.2)`，在不同组件间保持一致的视觉边界感。

### 2.6 阴影体系

| 层级     | 阴影值                                 | 应用            |
| -------- | -------------------------------------- | --------------- |
| 按钮发光 | `0 4px 12px rgba(66, 165, 245, 0.4)` | 主按钮 hover    |
| 浮动面板 | `0 8px 32px rgba(0, 0, 0, 0.5)`      | 无障碍/订单面板 |
| 登录卡片 | `0 12px 40px rgba(0, 0, 0, 0.4)`     | 登录注册卡片    |

### 2.7 订单状态色

| 状态   | 背景                         | 文字色      |
| ------ | ---------------------------- | ----------- |
| 待支付 | `rgba(255, 179, 0, 0.2)`   | `#ffb300` |
| 已完成 | `rgba(76, 175, 80, 0.2)`   | `#4caf50` |
| 已取消 | `rgba(158, 158, 158, 0.2)` | `#9e9e9e` |
| 已退票 | `rgba(244, 67, 54, 0.2)`   | `#f44336` |

### 2.8 无障碍模式色板

#### 高对比度模式

| 角色      | 色值        |
| --------- | ----------- |
| 背景      | `#000000` |
| 文字/边框 | `#ffffff` |
| 强调/标题 | `#ffff00` |
| 错误      | `#ff0000` |
| 成功      | `#00ff00` |

#### 色盲友好模式（基于 Okabe & Ito 调色板）

| 座位状态 | 填充色      | 辅助几何图案 |
| -------- | ----------- | ------------ |
| 空座     | `#0072B2` | 三点         |
| 选中     | `#E69F00` | 竖条纹       |
| 已售     | `#D55E00` | 十字         |
| 推荐     | `#56B4E9` | 圆环         |
| 已预订   | `#CC79A7` | 菱形         |

### 2.9 登录页专属配色

| 元素           | 色值                                          |
| -------------- | --------------------------------------------- |
| 卡片背景       | `#ffffff`                                   |
| 登录按钮渐变   | `linear-gradient(135deg, #43a047, #66bb6a)` |
| 注册按钮渐变   | `linear-gradient(135deg, #1e88e5, #42a5f5)` |
| Tab 激活下划线 | `#e53935`                                   |
| 输入框边框     | `#e0e0e0`                                   |
| 输入框聚焦     | `#e53935`                                   |

---

## 三、布局设计

### 3.1 页面级布局

采用经典三段式垂直布局，通过 Flexbox 实现：

```
┌──────────────────────────────────────┐
│              <header>                │  ← flex: 0 0 auto
├──────────────────────────────────────┤
│              <main>                  │  ← flex: 1（填充剩余空间）
│  ┌────────────┐  ┌──────┐          │
│  │  Canvas    │  │ Side │          │
│  │  座位图    │  │ bar  │          │  ← flex 水平两栏
│  └────────────┘  └──────┘          │
├──────────────────────────────────────┤
│              <footer>                │  ← flex: 0 0 auto
└──────────────────────────────────────┘
```

- **Body**：`display: flex; flex-direction: column; min-height: 100vh; background: #0a0a1a`
- **Main**：`display: flex; flex: 1`，Canvas 区域 `flex: 1`（自适应），Sidebar 固定 `width: 350px`

### 3.2 Header（顶部导航栏）

- Logo 居左，功能按钮组居右
- 功能按钮组：影厅选择器（小/中/大）、无障碍设置、我的订单、登出
- 布局方式：`display: flex; justify-content: space-between; align-items: center`
- 背景：`linear-gradient(135deg, #12122a, #1a1a3e)`

### 3.3 座位图区域（Canvas）

- Canvas 元素居中于 `.canvas-wrapper` 容器
- 容量自适应：通过 `resizeCanvas()` 动态计算尺寸
- 大字体模式下缩放系数为 1.25x
- 座位行号在 Canvas 内部绘制（大厅和中厅），小厅不显示座位号
- 屏幕（银幕）以圆弧形状绘制在座位上方

### 3.4 Sidebar（右侧功能边栏）

- 固定宽度 `350px`，左侧有边框分隔
- 顶部标题：`"选座工具"`
- 2x2 Grid 功能标签：智能推荐 / 手动选座 / 热度地图 / 体验评分
- 下方对应四个功能面板，通过 `.active` 类控制显示/隐藏
- 面板内表单元素统一使用 `display: flex; flex-direction: column` 纵向排列

### 3.5 Footer（底部栏）

- 水平两栏：左侧座位图例，右侧已选计数
- 图例项使用 `display: flex; gap: 22px` 水平排列
- 背景：`#12122a`

### 3.6 浮动面板

- 无障碍设置面板：`position: fixed; top: 60px; right: 28px`
- 我的订单面板：`position: fixed; top: 60px; right: 28px`
- 深色背景 `#1a1a3e`，圆角 `10px`，阴影 `0 8px 32px rgba(0,0,0,0.5)`
- 通过 `.show` 类控制显示，带动画过渡

### 3.7 登录页布局

- 全屏居中卡片式布局
- Header 为简化导航栏（仅 Logo + 无障碍按钮）
- Main 区域使用 Flexbox 垂直居中白色登录卡片
- 卡片内 Tab 切换（登录/注册），通过水平 Flex 实现
- Footer 为简化版权信息

### 3.8 响应式布局

| 断点                 | 行为                                                                            |
| -------------------- | ------------------------------------------------------------------------------- |
| `max-width: 860px` | Main 从水平两栏变为上下堆叠；Sidebar 宽度变为`100%`；Canvas 区域 padding 减小 |
| `max-width: 480px` | Header/Footer padding 缩小；按钮与字体等比例缩小；评分圆环尺寸减小              |

---

## 四、字体选择

### 4.1 字体栈

```css
font-family: "Microsoft YaHei", "PingFang SC", -apple-system, sans-serif;
```

按优先级依次回退：微软雅黑（Windows） → 苹方（macOS） → 系统 UI 字体 → 无衬线通用字体。选择理由：

- **微软雅黑**是 Windows 平台最广泛使用的中文无衬线字体，屏幕可读性优秀
- **苹方**是 macOS/iOS 默认中文字体，与系统风格一致
- **-apple-system** 回退确保在 Apple 设备上使用系统原生字体渲染

### 4.2 字号层级体系

| 层级        | 默认     | 大字体模式     | 主要应用                   |
| ----------- | -------- | -------------- | -------------------------- |
| H1/Logo     | `22px` | `28px`       | 页面主标题、Logo 文字      |
| H2/面板标题 | `16px` | `20px`       | Sidebar 标题、卡片标题     |
| H3/子标题   | `15px` | `20px`       | 浮动面板标题               |
| 正文        | `14px` | `18px`(body) | 按钮文字、选择器、功能标签 |
| 次要文字    | `13px` | `15px`       | 用户信息、表单标签、图例   |
| 辅助文字    | `12px` | `14px`       | 提示信息、订单号、评分说明 |
| 极小文字    | `11px` | `13px`       | 角色标签、星期标签         |

### 4.3 字重使用规范

| 字重     | 值              | 应用场景                                       |
| -------- | --------------- | ---------------------------------------------- |
| Bold     | `700`         | Logo、主按钮、统计数值、评分数字、Tab 激活标签 |
| Semibold | `600`         | 用户名、订单操作按钮                           |
| Medium   | `500`         | 表单标签                                       |
| Regular  | `400`（默认） | 正文文本、说明文字                             |

### 4.4 Canvas 内文字

Canvas 内部文字不依赖 CSS 字体系统，通过 `ctx.font` 直接指定：

| 元素     | 字体设置                                 | 字号               |
| -------- | ---------------------------------------- | ------------------ |
| 银幕标注 | `13px "Microsoft YaHei", sans-serif`   | 13px               |
| 行号标签 | `11px "Microsoft YaHei", sans-serif`   | 11px               |
| 座位号   | `8~10px "Microsoft YaHei", sans-serif` | 按座位尺寸动态计算 |

大字体模式下 Canvas 整体缩放 1.25x，上述字号同步放大。

---

## 五、组件设计规范

### 5.1 按钮体系

| 类型      | CSS 类             | 样式                                          | 悬停效果     |
| --------- | ------------------ | --------------------------------------------- | ------------ |
| 主按钮    | `.btn-primary`   | 蓝色渐变背景 + 白色文字                       | 蓝色发光阴影 |
| 次按钮    | `.btn-secondary` | 透明背景 + 蓝色边框 + 蓝色文字                | 浅蓝背景     |
| 危险按钮  | `.logout-btn`    | 透明背景 + 红色边框 + 红色文字                | 浅红背景     |
| 订单-支付 | —                 | `linear-gradient(135deg, #4caf50, #388e3c)` | 绿色发光     |
| 订单-退票 | —                 | `linear-gradient(135deg, #ef5350, #c62828)` | 红色发光     |
| 订单-取消 | —                 | `rgba(255,255,255,0.1)` 纯色                | 变亮         |

统一规范：`border-radius: 6px`，`transition: all 0.2s`，禁用态 `opacity: 0.5; cursor: not-allowed`。

### 5.2 输入框体系

- 背景：`#1a1a3e`
- 边框：`1px solid rgba(100, 140, 255, 0.2)`
- 圆角：`6px`
- 内边距：`6px 10px` ~ `10px 14px`
- 文字色：`#e0e0e0`
- 聚焦态：边框变为 `#64b5f6`，`outline: none`
- 占位符色：`#666`

### 5.3 Tab 切换

- **登录页 Tab**：水平 Flex 排列，底部有 `2px` 蓝色分隔线，激活项覆盖红色 `#e53935` 下划线
- **功能 Tab**：CSS Grid 2x2 网格，每个 Tab 为圆角按钮，激活态蓝色背景 + 蓝色边框

### 5.4 状态标签

- 角色标签 (`.role-tag`)：小圆角标签，半透明背景 + 对应色文字
- 订单状态 (`.order-status`)：`border-radius: 10px` 胶囊形标签

### 5.5 信息提示框

```css
.info-box {
    background: rgba(66, 165, 245, 0.08);
    border-left: 3px solid #64b5f6;
    padding: 10px 14px;
    border-radius: 0 6px 6px 0;
}
```

### 5.6 开关组件（Toggle）

- 外观：胶囊形轨道，白色圆形滑块
- 关闭态背景：`#555`
- 开启态背景：`linear-gradient(135deg, #42a5f5, #1e88e5)`
- 滑块通过 `::after` 伪元素 + `transform: translateX()` 实现位移动画
- 过渡：`background 0.2s` + `transform 0.2s`

### 5.7 Canvas 座位图

- 通过 Canvas 2D API 绘制，支持弧形银幕 + 多排圆角矩形座位
- 座位支持 5 种状态：空座(绿)、选中(黄)、已售(红)、推荐(蓝)、已预订(紫)
- 色盲模式下切换为 Okabe-Ito 调色板 + 几何图案双重编码
- 大字体模式下整体缩放 1.25x
- 鼠标悬停显示 Tooltip（`position: fixed` 跟随鼠标）

### 5.8 热度地图叠加层

- 在座位图上叠加半透明热度色，使用 Canvas `createRadialGradient` 实现渐变光晕
- 三档热度色：hot `#ef5350`、warm `#fbc02d`、cool `#42a5f5`
- 色盲模式下使用几何图案区分：hot 上三角、warm 菱形、cool 下三角
- 支持按星期切换数据，支持自动轮播

---

## 六、无障碍设计

### 6.1 四种辅助模式

| 模式     | 存储键值         | 实现方式                                                  |
| -------- | ---------------- | --------------------------------------------------------- |
| 大字体   | `largeText`    | body 添加`.large-text` class，全局字体 + Canvas 缩放    |
| 高对比度 | `highContrast` | body 添加`.high-contrast` class，覆盖全部颜色           |
| 色盲友好 | `colorblind`   | body 添加`.colorblind-mode` class，Canvas 切换色板+图案 |
| 语音提示 | `voice`        | Web Speech API，关键操作点播报                            |

### 6.2 设置持久化

所有无障碍设置存储在 `localStorage` 键 `smartCinemaA11ySettings` 中，页面加载时自动读取并应用。

### 6.3 ARIA 属性

- 无障碍面板：`role="dialog"`，`aria-label`
- Tab 组件：`role="tablist"` / `role="tab"` / `role="tabpanel"`，`aria-selected`
- 开关组件：`role="switch"`，`aria-checked`
- 评分组件：`role="radiogroup"` / `role="radio"`，`aria-label`

### 6.4 色盲友好图案规范

当座位尺寸 >= 8px 时显示图案，图案线宽为座位尺寸的 0.12~0.15 倍，元素尺寸为座位尺寸的 0.15~0.38 倍，图案颜色透明度设为 0.9 以保证对比度：

| 座位状态 | 图案   | 说明                 |
| -------- | ------ | -------------------- |
| 空座     | 三点   | 水平排列的三个小圆点 |
| 选中     | 竖条纹 | 多条垂直细线         |
| 已售     | 十字   | 交叉对角线           |
| 推荐     | 圆环   | 同心空心圆           |
| 已预订   | 菱形   | 旋转45度的正方形     |

热度地图图案（同样在 >= 8px 时显示）：

| 热度 | 图案   |
| ---- | ------ |
| 热门 | 上三角 |
| 一般 | 菱形   |
| 冷门 | 下三角 |

---

## 七、动效规范

### 7.1 过渡动画

| 属性     | 时长      | 缓动             | 应用范围                           |
| -------- | --------- | ---------------- | ---------------------------------- |
| 通用交互 | `0.2s`  | `ease`（默认） | 按钮 hover、输入框 focus、Tab 切换 |
| 侧边栏   | `0.3s`  | `ease`（默认） | 面板展开/收起                      |
| 进度条   | `0.35s` | `ease`         | 评分进度条宽度变化                 |
| 开关滑块 | `0.2s`  | `ease`（默认） | 滑块位移 + 背景色变化              |

### 7.2 Canvas 动画

- **推荐座位闪烁**：通过 `setInterval` 周期性切换推荐座位的透明度（`blinkPhase`），实现呼吸灯效果
- **热度地图轮播**：通过定时器自动切换热度数据日期（0-6 代表周一到周日），支持播放/暂停

---

## 八、总结

SmartCinema 设计体系以**深色科技风**为基调，通过**蓝色品牌主色 + 语义化功能色**构建清晰的信息层级，以 **Flexbox + Grid + Canvas** 三种布局技术分别处理页面结构、组件排列和图形绘制，辅以**四层无障碍模式**确保不同用户群体均可顺畅使用。色彩、字体、间距、动效均保持统一规范，确保两个页面（登录页和主界面）之间风格一致、体验连贯。
