# SmartCinema AI 协同开发报告

---

## 一、使用方式

全程采用“AI生成初稿 → 人工审查修改 → 交叉验证”模式，AI充当**高速编码助手与方案顾问**，所有架构决策、交互细节、视觉调优均由学生主导完成。

---

## 二、AI生成内容与学生修改总览

### 核心功能模块

| 模块 | AI主要贡献 | 学生关键修改 |
|------|-----------|-------------|
| 基础框架（登录/Canvas） | LocalStorage用户管理、极坐标弧形座位公式、响应式模板 | 调优弧跨度与座位密度；改为圆形碰撞；临时态（黄/蓝）保存时映射为空座 |
| 智能推荐 & 手动选座 | 硬约束过滤→连续空段扫描→评分排序框架；拖拽事件模板 | 团体票同排连续约束；少年/老年约束取并集；操作任意座位即清除推荐高亮 |
| 热度地图 & 体验评分 | 高斯热度模型、径向渐变光晕；三项指标评分+综合分公式 | 热度值clamp归一化；色盲模式Okabe-Ito色板+几何图案；支持多座位平均分与自动恢复 |
| 无障碍 & 订单中心 | 四开关面板、Web Speech封装；订单CRUD、BroadcastChannel多标签同步 | 补充全组件高对比样式；Canvas绘制色盲图案；订单用户隔离；仅广播全局状态变更 |
| AI顾问 & 交互增强 | 关键词对话引擎、动态权重调整；Shift框选/触摸滑动；独立动画层 | 生成自然语言推荐理由；完善多轮状态机与重置；矩形碰撞联动数据保存；动画帧率优化 |

### 关键设计决策

AI提供选项与依据，学生结合业务场景定夺：临时状态不持久化（刷新后不残留）；综合评分70%系统+30%观众权重；仅广播预订/支付/退票等全局变更，不同步个人临时选中；色盲适配增加几何图案双重编码。

### 典型问题解决

| 问题 | 解决方案 |
|------|----------|
| 大厅座位超出画布 | 实现自适应弧跨度与边缘余量 |
| 推荐无候选时崩溃 | 增加空段检查与友好错误提示 |
| 高对比样式不完整 | 用CSS变量统一管理，覆盖全部组件 |
| BroadcastChannel降级 | 增加LocalStorage storage事件后备 |
| 色盲图案小尺寸难辨 | 设置<8px仅用颜色，以上绘制图案 |

---

## 三、用户分析与体验优化

AI辅助生成五类用户画像（大学生、情侣、家庭、老年人、团体），指导推荐规则与界面设计。AI建议将选座流程压缩至3步以内，并增加悬停提示、语音播报、Toast通知等即时反馈。响应式断点参考AI建议，但具体折叠逻辑与触摸交互经学生实测调整。

---

## 四、学生独立思考体现

AI生成的评分公式偏理论化，学生通过实际测试调整权重与理想排位，使推荐更符合真实观影习惯。交互细节如“点击推荐座位自动清除其他推荐”、框选状态联动、变更队列防抖等，AI均未涉及，学生自主设计实现。视觉层面统一CSS变量体系，确保登录页与主界面主题同步。

---

## 五、总结

AI在代码生成、算法初稿、文档撰写上显著提升效率，但所有关键决策——交互逻辑、视觉风格、数据持久化策略——均由学生主导。AI是高效的“高级助手”，而**批判性审查、情境化调整与美学判断**始终是开发者不可替代的核心能力。
